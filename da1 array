array
1)sum of elementes
  #include <stdio.h>
int main()
{
    int arr[4]={3,4,7,1};
            int sum=0;
    for(int i=0;i<4;i++){
            sum=sum+arr[i];
    }
             printf("the sum of elementes are=%d",sum);

    return 0;
}
2)Find maximum and minimum element
  #include<stdio.h>
int main()
{
    int arr[4]={4 ,1 ,9 ,5};
    int i,max=arr[0];
    int min=arr[0];
    for (i=0;i<4;i++){
    if (arr[i]>max)
    max=arr[i];


    if (arr[i]<min)
    min=arr[i];
    }
    printf("maximum number is=%d \n",max);
     printf("minimum value=%d\n",min);

}
string
  3)#include <stdio.h>

int main()
{
    char name[20];
    int length = 0;

    printf("Enter a string:\n");
    scanf("%s", name);

    while (name[length] != '\0') {
        length++;
    }

    printf("Length of string = %d\n", length);

    return 0;
}



